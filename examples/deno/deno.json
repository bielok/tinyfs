{
  // Uncomment this line if you intend to use a version published in the last 24hs.
  // "minimumDependencyAge": 0,

  "tasks": {
    "build": "mkdir -p dist && deno bundle -o dist/app.js src/main.ts"
  },
  "imports": {
    "@bielok/tinyfs": "npm:@bielok/tinyfs"
  }
}
